import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    insightContainer: {marginHorizontal: 7.5},
    insightImage: {height: 210, width: 366, borderRadius: 5}
});

export default styles